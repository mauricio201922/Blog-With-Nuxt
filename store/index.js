// State
export const state = () => ({
    count: 0
});

// Mutation
export const mutation = {

}

// Action
export const action = {

}

// Getters
export const getters = {
    getCounter(state) {
        return state.count
    }
}